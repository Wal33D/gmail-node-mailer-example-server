{
  "name": "gmail-node-mailer-test-server",
  "version": "1.0.0",
  "description": "gmail-node-mailer-test-server",
  "main": "src/server.js",
  "engines": {
    "node": "18.18.2"
  },
  "scripts": {
    "predev": "node generateIndex.js",
    "dev": "nodemon --watch src --ext js,ts --exec \"ts-node src/server.ts\"",
    "prebuild": "node generateIndex.js",
    "build": "tsc",
    "prestart": "node generateIndex.js",
    "start": "node build/server.js",
    "clean": "rimraf dist/* --glob",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wal33D",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "dotenv-flow": "^3.2.0",
    "express": "^4.17.1",
    "gmail-node-mailer": "^1.8.5",
    "nodemon": "^3.1.0",
    "pdfkit": "^0.15.0",
    "stream-buffers": "^3.0.2"
  },
  "devDependencies": {
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ]
  }
}